import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'proghibr.peoresnada.gll',
  appName: 'proghibr-peoresnada-gll',
  webDir: 'www'
};

export default config;
